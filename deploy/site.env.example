# Copy to $HERMES_BRIDGE_ENV (default /etc/hermes-agent-bridge.env). Do not commit the filled file.
# Required:
SITE=site-a
PUBLIC_HOSTNAMES=mcp.example.com
CF_ACCESS_TEAM_DOMAIN=example.cloudflareaccess.com
CF_ACCESS_AUD=
HERMES_DASHBOARD_URL=
# Optional:
HERMES_DASHBOARD_TOKEN=
HERMES_API_KEY=
# HERMES_API_URL=
